# Release v1.3.8

Date: 2026-06-01

Summary:

- Bumped application version from `v1.3.3` to `v1.3.8` (patch +5).

Changes:

- Updated `APP_VERSION` constant in `spams/app/config/constants.php`.
  - Commit: 8617db0 (Bump APP_VERSION to v1.3.8)

Notes:

- This is a small patch-level bump; no database migrations expected.
- If you want a GitHub release created, tell me and I can create the release body.
